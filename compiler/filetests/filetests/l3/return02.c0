/*
(
 (kind (CompileAndRun))
)
*/

int gcd (int x, int y) {
  /* x,y >= 0 */
  int r;
  while (y != 0) {
    r = x%y;
    x = y;
    y = r;
  }
  return x;
}
  
int run() {
  return gcd(24,56);
}


void main() { assert (run() == (8)); }
----
