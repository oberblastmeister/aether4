/*
(
 (kind (CompileFail))
)
*/

int div (int x, int y) {
  int q;
  while (true) {
    if (x < y) {}
    q += 1;
    x -= y;
    x -= y;
  }
  return q; 			/* uninitialied variable! */
}

int run() {
  return div(127,3);
}


void main() {}
----
("Variable was used before initialized" (var q@2))