/*
(
 (kind (CompileAndRun))
)
*/

int sum (int n) {
  int i;
  int sum;
  sum = 0;
  for (i = 1; i <= n; i += 1)
    sum	+= i;
  return sum;
}

int main () {
  int result;
  result = sum(100);
  if (result == 100*(100+1)/2)
    return result;
  else
    return -1;
}
----
5050
