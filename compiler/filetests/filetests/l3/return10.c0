/*
(
 (kind (CompileAndRun))
)
*/

int fac(int n) {
  int k;
  k = 1;
  while (n > 1) {
    k *= n;
    n -= 1;
  }
  return k;
}

int run() {
  return fac(5);
}


void main() { assert (run() == (120)); }
----
