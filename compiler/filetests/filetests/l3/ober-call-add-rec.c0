/*
(
 (kind (CompileAndRun))
)
*/

int add(int x, int y) {
    if (x == 0) {
        return y;
    }
    return add(x - 1, y + 1);
}

int run() {
    return add(10, 4);
}


void main() { assert (run() == (14)); }
----
