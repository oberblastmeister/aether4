/*
(
 (kind (CompileAndRun)) 
)
*/

int run() {
    int first = 3;
    int second = 2;
    bool another = true;
    if (another) {
        first += 1;
    } else {
        first += second;
    }
    return first + second;
}


void main() { assert (run() == (6)); }
----
