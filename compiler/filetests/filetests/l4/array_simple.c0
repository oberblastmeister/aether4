/*
(
 (kind (CompileAndRun))
)
*/

typedef int_array int[];

void main() {
    int n = 100;
    int[] array = alloc_array(int, n);
    for (int i = 0; i < n; i++) {
        array[i] = i;
    }
    for (int i = 0; i < n; i++) {
        assert(array[i] == i);
    }
}
----
