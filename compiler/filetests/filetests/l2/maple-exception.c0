/*
(
 (kind (CompileAndRun (status (Signaled SIGFPE))))
)
*/

int run() {
  int prev = 0;
  int result = 0;
  int i;
  for (i = 1; i > prev; i <<= 1) {
    result |= i;
    prev = i;
  }
  return i / (result + i);
}


void main() { run(); }
----
