/*
( (kind (CompileFail)) )
*/

// return not in every path of execution
int run() {
  int i = 5;
  if(i == 5)
    return 4;
  else
    i += 2;
}


void main() {}
----
("Function does not return on all control flow paths" (func.name run@0))