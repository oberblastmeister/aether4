/*
( (kind (CompileFail)) )
*/

int main() {
	int n = 0;
	int i;
	for (i = 1; i <= 10; i ++)
	{
		n += i
	}
	return n;
}
----
("Parse error"
 ("expected closing brace for block"
  ("Stream.peek (Parser.stream env)" (((t (Ident n)) (span 10:3))))))