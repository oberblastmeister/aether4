/*
( (kind (CompileFail)) )
*/

int main() {
    int hello = 1234;
    int another = 12;
    int third = hello + another;
    third += another;
}
----
("Function does not return on all control flow paths" (func.name main@0))